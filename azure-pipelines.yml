# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'windows-latest'

steps:
- script: |
    echo --------- dir
    dir
    echo --------- docker build
    docker build -t yacoinbuild -f Dockerfile
  displayName: 'Build yacoin the painful way'
